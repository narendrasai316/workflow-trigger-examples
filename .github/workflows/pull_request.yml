name: pull request

on: 
  pull_request:
    branches:
      - "main"

jobs:
  test-push:
    runs-on: ubuntu-latest
    steps:
    - name: print
      id: echo
      run: |
        echo "this workflow trigered when you raised a PR into a main branch"
        
